{
    "builds": [
        {
            "src": "api/server/server.py",
            "use": "@vercel/python"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "api/server/server.py"
        }
    ]
}